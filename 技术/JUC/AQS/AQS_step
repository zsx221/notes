弄懂AQS分三个步骤。分为两部分：AQS=Status+CLH队列。
    场景一:(默认非公平锁)
        lock();
        使用CAS操作，把自己的状态update为1
        status为0的时候，会直接获取锁，并设置排他锁，设置为本次线程。
            而后执行业务逻辑之后，释放锁。

    场景二:

        status 不为0的时候，acquire(1),尝试获取，获取失败，再次请求加入队列，addWaiter()。

    场景三:

        status 不为0